#include<bits/stdc++.h>

// Method : 0
 void solve(int arr[]){
   int n=
 }

// Method 1
void move(int arr[],int s,int l)       // Time Complexity : O(n)   where n is the size of the array     Space Complexity : O(1) 
{
  int j=0;
  int n=sizeof()
  for(int i=0;i<n;i++)
  {
    if(arr[i]<0)
    {
      if(i!=j)
        swap(arr[i],arr[j]);
      j++;      
    }
  }
}

// Method : 2
void TwoPointer(int a[],int l,int r)
{
  
}
